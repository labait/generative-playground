services:
  caddy:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile.prod:/etc/caddy/Caddyfile:ro

  postgres:
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-laba}
